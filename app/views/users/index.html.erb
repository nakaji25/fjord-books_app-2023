<h1><%= t('views.common.title_index', name: i18n_pluralize(User.model_name.human)) %></h1>
<% @users.each do |user| %>
  <ul>
    <li><%= link_to t('views.users.show', name: user.name), user %></li>
  </ul>
<% end %>
<%= paginate @users %>
