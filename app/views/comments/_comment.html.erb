<div id="<%= dom_id comment %>">
  <p>
    <strong>content:</strong>
    <%= comment.content %>
  </p>
  <p>
    <strong>Date:</strong>
    <%= comment.created_at.strftime('%Y/%m/%d') %>
  </p>
  <p>
    <strong>Account:</strong>
    <%= comment.user.name %>
  </p>

</div>
